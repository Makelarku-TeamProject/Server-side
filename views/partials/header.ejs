<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.7.1/dist/jquery.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link rel="icon" href="https://grbhouse.cdn.prismic.io/grbhouse/4acba811-be93-4e0c-ba3f-46e2fd7d6c20_logo-house.svg"type="image/x-icon"/>
    <style>
        .property-card {
            border: none;
            border-radius: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s ease-in-out;
            overflow: hidden;
        }

        .property-card:hover {
            transform: translateY(-5px);
        }

        .property-card img {
            border-radius: 15px 15px 0 0;
        }

        .property-card .card-body {
            padding: 20px;
        }

        .property-card-title {
            font-size: 1.25rem;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .property-card p {
            margin-bottom: 15px;
        }

        .property-card .btn {
            background-color: rgba(252, 138, 49, 255);
            color: white;
        }

        .property-card .btn:hover {
            background-color: rgba(252, 110, 49, 255);
        }

        .property-details {
            font-size: 0.9rem;
            margin-bottom: 10px;
            color: #555;
        }

        .property-details i {
            color: rgba(252, 138, 49, 255);
            margin-right: 5px;
        }


        /* Custom media queries for extra styling */
        @media (max-width: 767px) {
            .hero {
                padding: 20px 0;
            }

            .hero img {
                width: 100%;
                height: auto;
            }

            .hero h2 {
                font-size: 1.5rem;
            }

            .hero p {
                font-size: 1rem;
            }

            .hero button {
                width: 100%;
                margin-top: 10px;
            }

            .card-section button {
                width: 100%;
                margin-top: 10px;
            }

            .campaign-banner button {
                width: 100%;
                margin-top: 10px;
            }
        }

        @media (min-width: 768px) and (max-width: 991px) {
            .hero h2 {
                font-size: 2rem;
            }

            .hero p {
                font-size: 1.2rem;
            }
        }
        
    </style>
    <title>House Property</title>
</head>